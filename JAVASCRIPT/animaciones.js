lottie.loadAnimation({
    container: document.getElementById('mi-animacion'), // Contenedor
    renderer: 'svg', // Usualmente 'svg' es el mejor renderizado para web
    loop: true, // Repetir animación (true para repetir, false para una vez)
    autoplay: true, // Autoplay habilitado
    path: './RECURSOS/ANIMACIONES/internet.json' // Ruta del archivo JSON
  });
  
  lottie.loadAnimation({
    container: document.getElementById('mi-animacion2'), // Contenedor
    renderer: 'svg', // Usualmente 'svg' es el mejor renderizado para web
    loop: true, // Repetir animación (true para repetir, false para una vez)
    autoplay: true, // Autoplay habilitado
    path: './RECURSOS/ANIMACIONES/telefonia.json' // Ruta del archivo JSON
  });
  
  lottie.loadAnimation({
    container: document.getElementById('mi-animacion3'), // Contenedor
    renderer: 'svg', // Usualmente 'svg' es el mejor renderizado para web
    loop: true, // Repetir animación (true para repetir, false para una vez)
    autoplay: true, // Autoplay habilitado
    path: '/RECURSOS/ANIMACIONES/click.json' // Ruta del archivo JSON
  });
  